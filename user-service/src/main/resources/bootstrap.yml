management:
  endpoints:
    web:
      exposure:
        include: "*" # actuator endpoint activation
        
spring:
  application:
    name: user-service

  cloud:
    config:
      uri: http://localhost:8888